extern crate popemodem;

use std::io::Error;

fn main() -> Result<(), Error> {
    // let config = ModemConfig::default();
    // let mut modem = Modem::new(config.clone());
    // modem.record();
    Ok(())
}
